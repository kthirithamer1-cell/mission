package com.projectmission.repository;

import com.projectmission.model.CompetitionInternationale;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CompetitionInternationaleRepository extends JpaRepository<CompetitionInternationale, Long> {
}
